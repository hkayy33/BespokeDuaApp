import Foundation

/// App Store product identifiers shared across StoreKit and the API client.
enum SubscriptionProducts: Sendable {
    /// Must match the product id in App Store Connect and the local StoreKit configuration file (if used).
    static let plusMonthlyID = "com.Stylistic.bespokeDua.subscription.monthly"
}
